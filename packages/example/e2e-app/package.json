{
  "name": "e2e-tests-with-auth0-simulator-container",
  "version": "1.0.0",
  "description": "E2E tests using the Auth0 Simulator container",
  "type": "module",
  "scripts": {
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "cypress:run:headed": "cypress run --headed",
    "test": "npm run cypress:run",
    "test:headed": "npm run cypress:run:headed",
    "dev": "npm run simulator"
  },
  "devDependencies": {
    "cypress": "^15.1.0",
    "typescript": "^5.9.2"
  }
}
